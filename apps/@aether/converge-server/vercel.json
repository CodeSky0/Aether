{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && if test \"$VERCEL_ENV\" = \"production\"; then echo '⏳ Running production migration...' && pnpm --filter @aether/db db:migrate || exit 1; else echo '⏭️  Skipping migration (not production)'; fi && pnpm turbo run build --filter=@aether/converge-server",
  "functions": {
    "api/ws.ts": {
      "memory": 1024,
      "maxDuration": 300
    }
  }
}
